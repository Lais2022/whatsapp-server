# ==============================================
# RENDER.YAML - Deploy Automático Render Free
# Cole este arquivo no seu repositório GitHub
# ==============================================

services:
  - type: web
    name: whatsapp-server
    runtime: docker
    # IMPORTANTE: aponta pro diretório do backend
    dockerContext: standalone-project/backend
    dockerfilePath: standalone-project/backend/Dockerfile
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: SELF_URL
        sync: false  # Preencha manualmente após o deploy com a URL do Render
